# DoneLayer BUILD_RESCUE reputation-entry fixture

This owner-controlled fixture has passing direct module tests but a broken public export surface.
The protected build verifier proves whether the package entry point exposes the required API.
