# DoneLayer Current Phase

## Current Status

- **GATE 4A-P CANONICAL ORCHESTRATION PREREQUISITE: LOCAL PASS; OWNER REVIEW REQUIRED.** The execute route now selects only server-registered workflows. A service-only canonical persistence boundary atomically validates one Work, locked Contract/hash, approved Authority/hash, exact repository/Commit/optional tree, Assignment, Agent identity, policy, and limits before creating a Job and immutable execution envelope. Finalization requires exact lineage, active Leases, complete Evidence, passed independent verification, verified cleanup, and zero unresolved policy violations; failure is append-only and idempotent.
- **GATE 4A-P LIVE MIGRATION IS NOT APPLIED.** A local PostgreSQL engine applied the complete ten-migration clean sequence, then a transactional runtime probe proved canonical prepare/replay, verifier-failure rejection, idempotent failure, and rollback to zero rows. Migration contract tests, authorization tests, duplicate/concurrency simulation, full repository tests, lint, strict typecheck, and Web/Worker builds also pass. The first live-validation step failed closed because Supabase CLI has no access token; no project was linked and no database change occurred. The migration must be applied and catalog/readback-tested on `donelayer-beta-validation` only after Owner supplies an authenticated CLI boundary.
- **THE APPROVED GATE 4A CANDIDATE IS UNCHANGED AND UNLOCKED.** `EXTERNAL REVIEW EXPORT BUNDLE V1`, DRAFT Contract fingerprint `b2a9189c76c29ca6081288c4083f92353be60dc9772877a383f8ce29c01fb9ab`, Authority fingerprint `341ebb78f90ac8595ed018971c63a74246b573a8ae5a62d6cd47ed9e6f3744b2`, and its exact three implementation files remain unchanged. Gate 4A-P created 0 Jobs, 0 corpus entries, 0 model requests, 0 Sandboxes, and 0 Reputation calculations. The proposed `GATE_4A_EXTERNAL_REVIEW_EXPORT_BUNDLE_V1` workflow is precheck-only until a separate Owner execution decision.
- **BETA GATE 3 REAL END-TO-END VERIFIED WORK: EXTERNALLY REVIEWED AND APPROVED.** External review returned `PASS WITH ONE NON-BLOCKING EVIDENCE-DEPTH CONDITION`; Gate 3 does not need a rerun. Execution is `COMPLETED`, independent verification is `VERIFIED`, delivery is `VERIFIED_DELIVERY`, and the Receipt is `VALID`. Classification remains separately `NOT_VERIFIED / BETA_GATE_VALIDATION`, with Reputation contribution `0`. Exact acceptance is in `BETA_GATE_3_EXTERNAL_REVIEW_ACCEPTANCE.md` and `test-results/beta-gate-3-external-review-acceptance.json`; the reviewed report and evidence hashes remain unchanged.
- **EXTERNAL_REVIEW_EVIDENCE_DEPTH_LIMITATION is recorded and non-blocking.** The supplied review bundle did not contain the raw 18-entry Ledger, canonical Receipt JSON, every Artifact manifest/byte payload, verification rows, both Sandbox lifecycle records, or raw authorization/readback transcripts needed for complete third-party recomputation. A complete external-review export is required before production/public third-party-verification claims; Gate 4A does not authorize that separate production-readiness work.
- **CURRENT GATE: GATE 4A - FIRST CANONICAL REAL JOB.** The Owner decision on 2026-09-13 resolves the earlier Gate 4 boundary ambiguity and authorizes the staged corpus ramp in `GATE_4_REAL_CORPUS_RAMP.md`: Gate 4A admits at least one externally accepted genuine Real Verified Job; Gate 4B reaches at least five across at least two pre-declared task types and validates duplicate/failure/revocation/supersession/contamination defenses; Gate 4C reaches at least twenty and passes `REPUTATION_READINESS_REVIEW_V1`. Only then may Phase 4 Evidence-Based Reputation start.
- **GATE 4A HAS NOT YET PASSED.** `REAL_CORPUS_ELIGIBILITY_RULES_V1` now has a verified local deterministic projection and integrity checker layered over `REAL_JOB_CLASSIFICATION_POLICY_V1`. This planning and policy implementation created no Job or corpus contribution. Provisional count remains `0`, active canonical count remains `0`, represented task types remain `0`, and the Gate 4C gap remains `20`.
- **BETA GATE 2 TRUST WORKSPACE V1: MILESTONE PASS - EXTERNAL REVIEW REQUIRED.** Two isolated test users completed the real application/Supabase Auth path and a durable, human-readable Agent -> Work -> locked Contract -> Authority -> status -> Evidence -> Receipt/history journey. Reload, sign-out/sign-in, revisit, desktop, mobile, controlled errors, and zero success-path console errors passed. Gate 2 deliberately created no Job Run, Permission Lease, execution Evidence, or Receipt. Exact evidence is in `BETA_GATE_2_TRUST_WORKSPACE_V1_REPORT.md` and `test-results/beta-gate-2-trust-workspace-v1-evidence.json`.
- **BETA GATE 2 MIGRATIONS ARE LIVE AND CATALOG VERIFIED.** Direct approvals authorized only `20260909131617_beta_gate_2_trust_workspace_v1.sql` at SHA-256 `f33138b1f79dedc43bc57cd2c58141ba30590d1b06c457cba62167fdca0426d6` and `20260909160114_beta_gate_2_workspace_bootstrap_fix_v1.sql` at SHA-256 `2291fd08e77d55f4dcc6efb124922203e635fe8f4910fe5a8be9b63fd8cce172` on `donelayer-beta-validation` (`blriuxocrzgwlojiduue`). Remote versions `20260909153232` and `20260909164614` are present. Catalog readback confirms bounded authenticated RPC grants, fixed function search paths, and the unambiguous `actor_auth_user_id` predicate. `gym` was not accessed or modified.
- **MILESTONE PASS - BETA GATE 1 REAL SUPABASE ENVIRONMENT - EXTERNAL REVIEW REQUIRED.** The dedicated non-production project is healthy on PostgreSQL 17 GA. Five exact-hash migrations, complete catalog checks, two-account database Auth/RLS isolation, service/user authorization, atomic rollback, stale/controller rejection, bounded revision and external-identity concurrency, UPDATE/DELETE append-only enforcement, reconnect durability, advisor review, and safe cleanup passed. Exact evidence is in `BETA_GATE_1_REAL_SUPABASE_ENVIRONMENT_REPORT.md` and `test-results/beta-gate-1-real-supabase-environment-evidence.json`.
- The Owner enabled leaked-password protection on the existing Pro-plan project without a plan change or purchase. The required first advisor rerun contains zero security warnings and only two intentional service-only `rls_enabled_no_policy` informational findings. Performance findings were recorded without scope expansion: 9 unindexed FKs, 58 unused indexes in the mostly empty project, 2 inherited multiple-policy warnings, and 1 Auth allocation notice.
- Final repository validation passed: 30/30 focused tests; a TypeScript 6 test-helper inference defect was preserved, narrowly fixed, and revalidated; lint and strict typecheck; 325 active tests across 66 files with 14 established skips across 7 files; Web/Worker builds; diff check; credential/path scan; exact migration hashes; and accepted historical evidence hashes/sidecars. One isolated controller Auth/Profile, two Agents, four identity revisions, and two ownership claims remain as minimal durable provenance; the unrelated identity and all failed-transaction rows are absent.
- The historical Gate 1 stop for external milestone review is resolved by the accepted review recorded above. Gate 1 remains a non-production engineering PASS, not production persistence activation, Private Beta readiness, or a Real Verified Job.
- The earlier Auth password-protection manual stop is preserved in `BETA_GATE_1_AUTH_PASSWORD_PROTECTION_BLOCKED.md` and is now resolved.
- **BETA GATE 1 SECURITY HARDENING V1 is LIVE and CATALOG VERIFIED.** Direct approval authorized SHA-256 `664d95d130691e0bc079054a1c745803d8f39f6eff15c946da5980d0fd608d1e`; remote version `20260909062033` applied successfully. The mutable-search-path warning is gone, semantic reviews are RLS-protected with no client policy, and function/table grants are service-only. The remaining security advisor notices are two intentional service-only `rls_enabled_no_policy` informational findings.
- **LIVE AUTH/RLS, ATOMICITY, CONCURRENCY, UPDATE IMMUTABILITY, AND RECONNECT CHECKS passed.** Two isolated Auth rows proved owner/unrelated/anonymous read isolation. Ordinary mutation and direct writes failed `42501`; cross-account service commands failed closed. Invalid Agent creation rolled back every child/audit row. Stale and controller-changing revisions failed. Competing revision and external-identity requests each produced one winner, one explicit conflict, no overwrite, and no losing child rows. Service-role UPDATE probes reached Postgres and failed `55000` on both append-only identity tables.
- The earlier DELETE-probe authorization stop is preserved in `BETA_GATE_1_DELETE_NEGATIVE_PROBES_AUTH_BLOCKED.md` and is now resolved. Both DELETE probes failed closed as designed, and unrelated test identity cleanup completed. The final evidence, repository regressions, and leakage scan that were pending at that historical stop have since passed as recorded above.
- **SEMANTIC VERIFICATION V1 and AGENT IDENTITY PRODUCTION PERSISTENCE V1 are LIVE and CATALOG VERIFIED on the dedicated non-production project.** The Owner directly approved their exact hashes; remote versions `20260909054834` and `20260909054923` applied successfully after the prerequisite. Expected tables, constraints, indexes, functions, triggers, RLS configuration, grants, and zero initial rows were read back. The prior authorization blocker remains preserved historical evidence.
- The earlier security-hardening approval stop is preserved in `BETA_GATE_1_SECURITY_HARDENING_AUTH_BLOCKED.md` and is now resolved. The two safe test identities and bounded validation rows were created only after hardening passed catalog and advisor readback.
- **TRUST FOUNDATION SUPABASE PREREQUISITE MIGRATION V1 is LIVE and CATALOG VERIFIED on the dedicated non-production project.** Direct approval authorized exact SHA-256 `ae62be0c0144b25a3590cfcf800abe419ef131e757fe2922426c1d798bcd1871`; remote version `20260909050427` applied successfully. All four tables, the Job Contract binding column, expected constraints/indexes, 16 protection trigger events, RLS, participant read policies, and explicit grants were independently read back. Foundation Job/Lease/Ledger/Receipt tables remain empty; one isolated non-production controller Auth row remains solely for durable identity provenance.
- The earlier `semantic_verification_v1` application was rejected before tool execution because direct approval then named only the prerequisite. That blocker is preserved in `BETA_GATE_1_REMAINING_MIGRATIONS_AUTH_BLOCKED.md` and `test-results/beta-gate-1-remaining-migrations-auth-blocked.json`; it was later resolved by direct Owner approval, and both reviewed migrations are now applied as recorded above.
- The earlier manual action requiring direct approval of both reviewed migration hashes is resolved and is not the current stop. `gym` remains excluded and untouched. The Gate remains `NOT_VERIFIED`; provisional and canonical contributions are zero.
- **TRUST FOUNDATION SUPABASE PREREQUISITE MIGRATION V1 local authorization blocker is preserved historical evidence and is now resolved.** Immutable pre-semantic Gate databases proved the exact four-table Trust Foundation boundary and old Receipt/Ledger value sets. The new migration `20260903080000_trust_foundation_prerequisite_v1.sql` has SHA-256 `ae62be0c0144b25a3590cfcf800abe419ef131e757fe2922426c1d798bcd1871`; 5/5 focused tests prove clean-install order, unchanged historical hashes, bounded scope, binding and immutability triggers, RLS, and explicit grants.
- The execution safety layer previously rejected prerequisite `apply_migration` before it reached Supabase because approval was provided through an attachment instead of direct chat. No workaround was attempted. That historical state contained only `initial_schema`, zero Auth users, and no Trust Foundation tables. Exact evidence is in `BETA_GATE_1_PREREQUISITE_APPLICATION_AUTH_BLOCKED.md` and `test-results/beta-gate-1-prerequisite-application-auth-blocked.json`.
- The Owner later supplied the required direct approval, so that prerequisite stop is resolved. `gym` remains excluded and untouched. The Gate remains `NOT_VERIFIED`; provisional and canonical contributions are zero.
- **BETA GATE 1 ORIGINAL MIGRATION FAILURE is a preserved historical `OWNER_DECISION_REQUIRED` result, now superseded operationally by the approved prerequisite strategy.** The Owner-authorized disposable project `donelayer-beta-validation` (`blriuxocrzgwlojiduue`) passed read-only preflight on PostgreSQL 17.6 with an empty migration history, zero public tables, zero Auth users, and no unrelated data. The initial schema applied successfully. The next historical migration, `semantic_verification_v1`, failed and rolled back with PostgreSQL `42P01` because `job_receipts` does not exist.
- The preceding Supabase schema omits the trust-foundation table family required by that semantic migration. No historical migration was edited or skipped, the Agent Identity migration was not attempted, and no Auth account or test row was created. Live identity/RLS/concurrency/advisor claims remain unproven. Exact evidence is in `BETA_GATE_1_REAL_SUPABASE_MIGRATION_BLOCKED.md` and `test-results/beta-gate-1-real-supabase-migration-blocked.json`.
- The unrelated `gym` project was not accessed project-specifically or mutated. The new validation project remains in place as authorized. The historical Gate result is `NOT_VERIFIED`, with provisional and canonical contributions of zero. The Owner approved the prerequisite strategy in the later decision recorded above.
- **AGENT IDENTITY PRODUCTION PERSISTENCE V1 LOCAL CONTRACT GATE is OWNER ACCEPTED.** The accepted scope is strictly the migration/schema contract, append-only hashed identity revisions, controller continuity, immutable global external identity ownership, RLS design, service-only atomic Agent creation, SQLite compatibility, and local deterministic validation. The mode-aware server adapter connects Agent create/read/revise/link APIs while retaining SQLite behavior outside Supabase mode.
- The accepted report remains a local-contract result. Beta Gate 1 subsequently applied the Agent Identity migration and passed live Auth/RLS, atomicity, competing-writer, durability, cleanup, advisor, and complete regression checks in the dedicated non-production project. The rest of the Marketplace service layer and Storage still use SQLite. Exact accepted local scope is in `AGENT_IDENTITY_PRODUCTION_PERSISTENCE_V1_REPORT.md`; the later live result is in `BETA_GATE_1_REAL_SUPABASE_ENVIRONMENT_REPORT.md`.
- This genuine roadmap task remains `NOT_VERIFIED`: it did not run as a full current Agent Job or produce a Verified Job Receipt. Provisional and canonical contributions remain zero, the Reputation gap remains 20, and no Real Job audit batch exists.
- **AGENT IDENTITY DURABLE PRODUCTIZATION V1 is COMPLETE and OWNER ACCEPTED at its recorded local SQLite product boundary.** Every Marketplace and platform-managed Agent receives an append-only identity revision in the same transaction; controller-only revision/history/link APIs and a Receipt/Authority-derived profile view are connected. Demo counters and hard-coded completion history are not treated as proof.
- The new persistent evidence verifies five Agents, seven revisions, one immutable declared external identity, three linked evidence-Agent revisions, four denied probes, and exact restart recovery. Full validation passed: 305 active tests across 62 files, 14 established skips, lint, strict typecheck, Web/Worker build, HTTP 201/200/409/403 behavior, desktop/mobile browser QA, diff check, and leakage scan.
- The accepted SQLite milestone is not Supabase/multi-instance production persistence, legal identity assurance, live external verification, deployment, or Reputation. It remains `NOT_VERIFIED` because it did not run through a full current Agent Job lifecycle or produce a Verified Job Receipt; provisional and canonical contributions are both zero. Exact evidence is in `AGENT_IDENTITY_DURABLE_PRODUCTIZATION_V1_REPORT.md`.
- **OWNER PROCESS CORRECTION:** a major milestone with zero provisional Jobs requires only `MILESTONE PASS - EXTERNAL REVIEW REQUIRED`. Emit `OWNER DECISION REQUIRED - EXTERNAL REAL JOB AUDIT BATCH` only when provisional work actually requires external classification.
- **REAL JOB CLASSIFICATION AND EXTERNAL AUDIT POLICY V1 is PASS at a local deterministic boundary.** Four preliminary states are enforced: `BENCHMARK_FIXTURE`, `NOT_VERIFIED`, `PROVISIONAL_REAL_JOB`, and `AMBIGUOUS`. Benchmark and unverified work is excluded automatically; provisional and ambiguous work contributes zero and enters `docs/REAL_JOB_AUDIT_QUEUE.md`.
- Only an externally accepted provisional record can become `OWNER_REAL_VERIFIED_JOB` or `EXTERNAL_REAL_VERIFIED_JOB` and add one to the canonical count. Audit triggers at five new provisional Jobs, an apparent threshold crossing, blocking ambiguity, or a major canonical milestone when at least one provisional entry actually requires classification. Unknown values, extra fields, tampering, invalid audit decisions, and non-provisional promotion fail closed.
- Attempt 5 now has an append-only owner-classification artifact bound to its unchanged evidence/database hashes. `TEST_AND_FIX` is `BENCHMARK_FIXTURE_VERIFIED_JOB`; `BUILD_RESCUE` is `NON_VERIFIED_BENCHMARK_ATTEMPT`; queue additions and canonical contribution are zero. The classification artifact evidence SHA-256 is `ce239b19ad025389ec9657f1d05cc24dce82dfe2422f2d6db2753666ce61dfc6`.
- Validation passed: 13/13 focused tests, lint, strict typecheck, 289 active tests across 59 files, Web build, Worker build, `git diff --check`, credential/path scan, historical hashes, and no Attempt 3/5 WAL/SHM. This Gate made zero model calls, Sandboxes, Jobs, promotions, or external mutations. Exact scope is in `REAL_JOB_CLASSIFICATION_POLICY_V1_REPORT.md`.
- **PHASE B ATTEMPT 5 is complete with `PARTIAL`: one eligible `VERIFIED_DELIVERY` candidate and one `INCONCLUSIVE` Job.** Stage 0 passed before lifecycle creation: refreshed development OIDC was structurally valid/current/exactly bound to `nickhois-projects/donelayer`, the non-model Gateway catalog exposed three eligible zero-price models, both approved V3 refs resolved to exact Commits, Source materialization passed, and all temporary workspaces were cleaned.
- `TEST_AND_FIX` used seven model requests and completed eight controlled tool calls before `AI_GATEWAY_MODEL_REQUEST_FAILED`. It changed only `src/retry.ts`; patch SHA-256 `c61a9eff7bf269176a2cbb244335902053053a7dde9607e866887e7b9b7294f2`. A fresh independent Sandbox passed the locked verifier. Execution is truthfully `PROVIDER_FAILURE` / `MODEL_PROVIDER`, verification is `VERIFIED`, and the pre-locked `INDEPENDENT_ACCEPTANCE_SUFFICIENT` policy yields `VERIFIED_DELIVERY`.
- `BUILD_RESCUE` used two model requests, then the same provider failure produced no tool call or patch. Independent verification is `NO_DELIVERABLE`; final delivery is `INCONCLUSIVE`. It is not eligible and must not be rerun or relabeled as success.
- Attempt 5 used `9 / 16` model requests and `4 / 4` non-persistent Vercel Sandboxes. All four Sandboxes are stopped, cleanup-verified, and snapshot-free; both Leases are `REVOKED`. The SQLite store verifies 81 records, 78 lifecycle entries, and both Jobs. Lint, strict typecheck, 276 active tests, Web build, Worker build, `git diff --check`, and credential/path scanning passed.
- **OWNER CLASSIFICATION: `BENCHMARK_FIXTURE_VERIFIED_JOB`, DO NOT COUNT.** Attempt 5 has complete durable Bundle/Ledger/Receipt evidence and one technically eligible candidate, but its task was intentionally created primarily to exercise the corpus pipeline. It remains `counted: false` and `reputationEntered: false`. `BUILD_RESCUE` is a `NON_VERIFIED_BENCHMARK_ATTEMPT`. Canonical count remains `0`, gap remains `20`, qualifying task types remain none, and Reputation has not started.
- The permanent four-state preliminary policy is `REAL_JOB_CLASSIFICATION_POLICY_V1`: `BENCHMARK_FIXTURE`, `NOT_VERIFIED`, `PROVISIONAL_REAL_JOB`, or `AMBIGUOUS`. Benchmark and unverified Jobs are excluded automatically. Provisional and ambiguous Jobs enter `docs/REAL_JOB_AUDIT_QUEUE.md` and contribute zero until external audit acceptance. Audit is triggered at five new provisional Jobs, a major canonical milestone, an apparent threshold crossing, or a blocking ambiguity.
- Attempt 3/4 hashes remain byte-identical. Exact Attempt 5 evidence is in `REPUTATION_ENTRY_PHASE_B_ATTEMPT_5_REPORT.md`, the five `test-results/reputation-entry-phase-b-attempt-5*` artifacts, `docs/REAL_JOB_CLASSIFICATION_POLICY.md`, and `docs/REAL_JOB_AUDIT_QUEUE.md`. Attempt 5 is closed; do not rerun either Job or begin Reputation. Any new live corpus Gate still requires a separately authorized execution boundary.
- **PHASE B ATTEMPT 4 is FAIL / BLOCKED before Job creation.** Both new V3 Fixture refs materialized at exact commits `abf1bc277a7c27a4b50ab32033cf5d3ef8ee7728` and `746fa8ba7cef938a6245be3952e97f8697dfd2ca`, with verified source cleanup and no Stage 0 mutation. The development credential then failed OIDC inspection with `AI_GATEWAY_OIDC_EXPIRED_OR_NOT_ACTIVE`, so the Gateway catalog was not queried and no Job database, Contract, Lease, execution, model request, Sandbox, Bundle, Ledger, Receipt, delivery, or candidate exists.
- Fixture preparation created only the two authorized owner-controlled branches/commits and no PR. Attempts 1–3 and Persistence Attempts 1–4 remain immutable; Phase B Attempt 3 still has outcomes `FAILED` and `INCONCLUSIVE`, zero verified candidates, canonical count `0`, and gap `20`. Exact evidence is in `REPUTATION_ENTRY_PHASE_B_ATTEMPT_4_BLOCKED.md` and `test-results/reputation-entry-phase-b-attempt-4-preflight.json`.
- Do not overwrite or retry Attempt 4. The exact manual step is `vercel.cmd env pull .env.local --environment=development`; after refresh, a separately identified new Attempt requires explicit Owner approval. Evidence-Based Reputation remains unauthorized.
- **VERIFIED DELIVERY V2 LIVE-ORCHESTRATOR PRE-LIVE GATE is PASS.** Newly issued `VERIFIED_WORK_CONTRACT_V2` Contracts now carry their locked delivery policy through the real Phase B orchestration path, with execution, independent verification, and delivery persisted separately into Bundle V2, Ledger, Receipt V2, public projection, and delivery-only candidate eligibility. V1 Contracts retain historical semantics.
- The schema-family dispatcher now validates shared durable record types by exact hash-covered `bundleType`/`receiptType` family plus record version. The repository-proven families are Persistence Preflight V1, Reputation Entry V1, and Reputation Entry Outcome V2; missing, unknown, conflicting, relabeled, ambiguous, upgraded, or downcast family identity fails closed. The focused family matrix passed 18/18 and the V2 orchestrator matrix passed 2/2 while covering all accepted outcome vectors.
- Complete validation passed: both immutable historical validations, lint, strict typecheck, 273/273 active tests across 54 files, Web production build, Worker build, and `git diff --check`. Attempt 4 remains byte-identical at database/evidence SHA-256 `280d616ec1366ab00d81962b73fb5dfefe9e0b97fe9b71a64dbac20afeae8162` / `ea6a994968b93cd1dac2a192e8cb5cf6d409b4d11fc661a87237cd2850401b36`; Attempt 3 remains byte-identical at `30da49af05e27b7c29e07015d3eaf705809cef74000481ed78f58125e1e7fb37` / `49e54b83129310603bea9d3ac93301b528426e0036f00ac07c7c934374793077`. Both stores remain WAL/SHM-free.
- This clean rerun made zero actual model calls, created zero managed Sandboxes or live Jobs, retained no deterministic test Job state, and caused no external, promotion, or Reputation effects. Exact results are in `VERIFIED_DELIVERY_V2_LIVE_ORCHESTRATOR_PRELIVE_REPORT.md`. Stop for Owner review; a new live Phase B corpus Attempt requires a separate explicit decision.
- **V1 SCHEMA-FAMILY DISPATCHER CORRECTION + COMPLETE V2 PRE-LIVE RERUN is BLOCKED.** Exact historical inspection proved three unambiguous families using existing hash-covered `bundleType` and `receiptType` literals. The family-aware correction restored retained Attempt 4 validation and strict typecheck passed.
- The mandatory Attempt 3 test then failed only at its final no-sidecar assertion because raw SQLite shape inspection had created a zero-byte WAL and 32 KiB SHM before the test. Both transient sidecars were removed; the database stayed byte-identical at SHA-256 `30da49af05e27b7c29e07015d3eaf705809cef74000481ed78f58125e1e7fb37`, WAL/SHM are again absent, and Attempt 3 remains `PARTIAL / BLOCKED` with immutable outcomes `FAILED` and `INCONCLUSIVE`.
- The Gate stopped at that failure. The remaining focused family matrix, V2 matrix rerun, lint, full suite, Web build, Worker build, and final diff check were not run. No model, managed Sandbox, live Job, external mutation, promotion, or Reputation effect occurred. Exact evidence and the next Owner decision are in `VERIFIED_DELIVERY_V2_SCHEMA_FAMILY_RERUN_BLOCKED.md`; prior reports remain unchanged.
- **VERIFIED DELIVERY V2 LIVE-ORCHESTRATOR PRE-LIVE GATE is BLOCKED.** The V2-only orchestration branch, separate execution/verification/delivery persistence, Bundle V2, Ledger event, Receipt V2, public projection, and delivery-only eligibility passed 24/24 focused checks; strict typecheck, lint, `git diff --check`, and 2/2 read-only Attempt 3 checks also passed.
- The complete suite stopped at one retained Phase A Attempt 4 compatibility failure: the new durable dispatcher incorrectly applies Reputation-entry-specific V1 Bundle and Receipt schemas to the historical `PERSISTENCE_PREFLIGHT_EVIDENCE_BUNDLE_V1` family. Read-only diagnosis reports `DURABLE_EVIDENCE_BUNDLE_V1_INVALID` and `DURABLE_JOB_RECEIPT_V1_INVALID`; 254 tests passed, one failed, and 11 remained skipped. Web and Worker builds were not run after the fail-closed stop.
- Attempt 3 remains byte-identical and classified `PARTIAL / BLOCKED`; no live Job, model call, managed Sandbox, external mutation, candidate promotion, or Reputation work occurred. Exact results and the next required Owner decision are in `VERIFIED_DELIVERY_V2_LIVE_ORCHESTRATOR_PRELIVE_BLOCKED.md`.
- **VERIFIED DELIVERY OUTCOME POLICY V1 DESIGN AND EVIDENCE GATE is PASS.** The Owner accepted prospective Model C. New `VERIFIED_WORK_CONTRACT_V2` Contracts lock one hash-covered policy before execution: `EXECUTION_AND_INDEPENDENT_ACCEPTANCE_REQUIRED` or `INDEPENDENT_ACCEPTANCE_SUFFICIENT`. Historical V1 Contracts have no default or inferred policy and keep their original meaning.
- The V1 evaluator now records Agent execution, explicit failure attribution, independent work verification, and final Contract delivery separately. `VERIFIED_DELIVERY` requires an authorized deliverable, verified independent acceptance, complete required Evidence, valid Source/provenance, valid Authority and lifecycle, verified cleanup, and compliant external effects. The strict policy additionally requires completed Agent execution.
- Prospective Evidence Bundle V2, Ledger payload, Receipt V2, candidate projection, public Receipt type/projection, and Receipt UI preserve all three outcomes. A failed execution may remain visible beside `VERIFIED` work and `VERIFIED_DELIVERY`; workflow membership alone never qualifies a candidate and no Receipt from this Gate is canonically counted.
- The mandatory matrix passed 19/19 deterministic tests, and the generated-evidence replay passed 1/1, including the Attempt 3-equivalent control vector under both policies, no-deliverable safety, every required trust failure, policy/version tampering, V1 compatibility, immutable Contract/template binding, truthful Receipt projection, and delivery-based candidate eligibility. Complete validation passed: lint, strict typecheck, 253/253 active tests across 52 passing files, Web production build, Worker build, and `git diff --check`.
- Attempt 3 was not re-evaluated, migrated, regenerated, promoted, or rerun. Its five accepted Artifact hashes remain byte-identical, its database has no WAL/SHM, `TEST_AND_FIX` remains `FAILED`, `BUILD_RESCUE` remains `INCONCLUSIVE`, and canonical real Verified Job count remains `0` with a gap of `20`.
- This Gate made zero model calls, created zero Sandboxes, and caused no GitHub, Fixture, PR, payment, deployment, blockchain, billing, candidate-promotion, or Reputation effect. Exact evidence is in `VERIFIED_DELIVERY_OUTCOME_POLICY_V1_REPORT.md` and `test-results/verified-delivery-outcome-policy-v1-evidence.json`. Stop for Owner review; no live corpus Attempt or Reputation work is authorized.
- **ATTEMPT 3 LIFECYCLE EVIDENCE — VALIDATED.** The owner-authorized version-aware lifecycle-verifier correction now resolves each lifecycle entry to exactly one durable record by record type, record ID, and the already-authoritative payload SHA-256, then validates the preserved `record_version` against strict `AGENT_MODEL_RUN` V1 or V2 schemas. Missing, relabeled, invalid, or unknown versions fail closed; historical payloads are not upgraded or downcast.
- The corrected canonical store opened the immutable Attempt 3 database with SQLite `readOnly: true` and `immutable=1`. `verifyAll()` now returns `valid: true` for 79 durable records, 76 lifecycle entries, and both Jobs. The database SHA-256 before and after is the same `30da49af05e27b7c29e07015d3eaf705809cef74000481ed78f58125e1e7fb37`; no WAL/SHM remains and no row, hash, lifecycle entry, Receipt, or other Attempt 3 artifact changed.
- `TEST_AND_FIX` has a valid 47-entry lifecycle with V1/V2 model-run records and seven persisted requests. Its Contract, Authority, Bundle, Ledger, Receipt, and both cleanup records validate, but its immutable Job/Receipt result remains `FAILED` and `VERIFIED_DELIVERY` remains not achieved. `BUILD_RESCUE` has a valid 29-entry lifecycle with V1/V2 model-run records and two persisted requests; the same applicable bindings validate, but its immutable Job/Receipt result remains `INCONCLUSIVE` and `VERIFIED_DELIVERY` remains not achieved.
- `REPUTATION_ENTRY_CANDIDATE` is explicitly classified as `CANDIDATE_COLLECTION_WORKFLOW_MEMBERSHIP_ONLY`, not a successful qualification. Verified-candidate counting still requires result `VERIFIED`; both Receipts remain canonically uncounted and require Owner definition acceptance plus explicit promotion. Verified candidates, canonical count added, and canonical real Verified Jobs all remain `0`; the gap remains `20` and Evidence-Based Reputation has not started.
- Validation passed: 25/25 focused version-aware and Attempt 3 checks, lint, strict typecheck, 233/233 active full-suite tests, Web production build, Worker build, and `git diff --check`. This correction made zero model calls, created zero Sandboxes, and caused no external effects.
- Exact correction and read-only evidence are in `REPUTATION_ENTRY_PHASE_B_ATTEMPT_3_LIFECYCLE_REVALIDATION_REPORT.md`. That accepted stop point did not authorize an Attempt 4, Job rerun, candidate promotion, or Reputation work.
- **PHASE B ATTEMPT 3 is PARTIAL / BLOCKED.** Its new pre-lifecycle Source/OIDC/provider preflight passed before the Attempt 3 database existed: the development OIDC was structurally valid, current through `2026-09-05T02:51:15.000Z`, exactly bound to linked project `nickhois-projects/donelayer`, the authenticated non-model Gateway catalog check found two eligible zero-price models, both approved V2 refs resolved to their exact approved commits, all materializer workspaces were cleaned, and no preflight mutation occurred.
- Both authorized Jobs ran. `TEST_AND_FIX` made seven model requests, completed eight controlled tool calls, produced the exact one-file repair, and passed fresh independent verification, but its model run ended `FAILED`, so the Job and Receipt are `FAILED`. `BUILD_RESCUE` made two model requests, produced no tool call or patch, and is `INCONCLUSIVE`; its fresh verifier correctly retained the failing baseline. The batch decision is `PARTIAL` with zero verified candidates.
- Attempt 3 used `9 / 16` model requests and `4 / 4` non-persistent Vercel Sandboxes. All four Sandboxes are stopped, cleanup-verified, and snapshot-free. Both Leases are `REVOKED`. No PR, merge, deployment, real payment, blockchain write, billing change, production credential expansion, or Fixture mutation occurred.
- Both Job Evidence Bundles are complete; both Job-level Ledgers and non-success Receipts recompute as valid. The Attempt 3 evidence database is nevertheless blocked because the built-in durable recovery verifier drops `record_version` when it binds the two versioned `AGENT_MODEL_RUN` records, so `verifyAll()` returns `valid: false`. A separate read-only version-aware recomputation found both cryptographic lifecycle chains and exact payload bindings valid; this does not override the canonical verifier failure or authorize evidence mutation.
- `VERIFIED_DELIVERY` is not achieved. Both records retain the `REPUTATION_ENTRY_CANDIDATE` field, but verified candidate count, canonical count added, and canonical qualifying total before Owner classification are all `0`; the gap remains `20`. Reputation entry conditions did not change and Evidence-Based Reputation did not start.
- **PHASE B ATTEMPT 2 — HISTORICAL FAIL / BLOCKED** is permanent. Attempts 1 and 2 remained byte-identical and were not retried or overwritten. Exact Attempt 3 evidence and the next required Owner decision are in `REPUTATION_ENTRY_PHASE_B_ATTEMPT_3_BLOCKED.md`.
- The lifecycle-verifier correction and read-only revalidation were later approved and completed without a Job rerun, Attempt 4, candidate promotion, or Reputation implementation.
- **PHASE B ATTEMPT 2 is FAIL / BLOCKED at the Agent provider authentication preflight.** Its mandatory Stage 0 passed in the exact Node/Vitest materializer process for both approved refs and commits; all three Source materialization workspaces were cleaned, and no remote mutation occurred.
- Job 1 legitimately began, persisted its new Agent profile, Job identity, exact Source identity, and V2 Work Contract, then stopped before execution identity, Permission Lease, model request, or Sandbox creation. The configured development Vercel OIDC token was correctly bound to `project:donelayer:` but had expired at `2026-09-04T01:09:53.000Z`.
- Attempt 2 made zero model requests, created zero Sandboxes, issued no Lease, and produced no patch, verifier result, final Bundle, Ledger, Receipt, or candidate. Job 2 did not start. Canonical real Verified Jobs remain `0`; Evidence-Based Reputation remains not started.
- Attempt 2 evidence is preserved in `REPUTATION_ENTRY_PHASE_B_ATTEMPT_2_BLOCKED.md`, `test-results/reputation-entry-phase-b-attempt-2-source-preflight.json`, `test-results/reputation-entry-phase-b-attempt-2.sqlite`, and `test-results/reputation-entry-phase-b-attempt-2-working-tree-identity.txt`. No retry is authorized. The one manual next step is to refresh `.env.local` with `vercel env pull .env.local --environment=development`, after which the Owner must issue a separate execution decision.
- **PHASE B ATTEMPT 1 is owner-classified as HISTORICAL FAIL / BLOCKED; PHASE B ATTEMPT 2 is authorized only behind a read-only Source-materialization preflight.** Attempt 1 failed before Agent execution during GitHub Source clone, created zero Sandboxes and zero model requests, and is not a candidate or Agent capability signal. Its partial database, working-tree identity, lifecycle, hashes, and report are immutable historical evidence.
- Attempt 2 may reuse the two approved V2 Fixture commits, but it must first exercise the same `materializeVerifiedSourcePackage` process path for both exact refs and commits, persist PASS/BLOCKED preflight evidence, verify workspace cleanup, and perform no remote mutation. No Attempt 2 Job database may exist unless that Stage 0 preflight passes.
- If Stage 0 passes, Attempt 2 may run exactly one `TEST_AND_FIX` Job and one `BUILD_RESCUE` Job, with at most four nonpersistent Vercel Sandboxes and sixteen durably counted model requests. Each Job requires a fresh execution, V2 Contract, scoped Lease, repair Sandbox, independent verifier Sandbox, Evidence Bundle, Ledger, Receipt, Lease revocation, and cleanup.
- The Attempt 1 profile is batch-local rather than independently proven in a durable platform identity registry, so Attempt 2 must create a new ACTIVE owner-controlled V1 profile. Canonical real Verified Jobs remain `0`; Evidence-Based Reputation remains not started.
- Stop after Attempt 2 for Owner review. No third Job, further batch, Reputation work, PR, merge, deployment, real payment, blockchain action, billing change, Snapshot, arbitrary repository access, or production credential expansion is authorized.
- **PRE-LIVE REGRESSION GATE is PASS after the authorized dead-helper cleanup. PHASE B LIVE EXECUTION remains NOT AUTHORIZED.** The unused private `taskDefinition` runtime wrapper was removed, and the exported helper now uses the existing explicit `ReputationEntryTaskDefinition` return type. No semantic versioning behavior changed.
- Complete validation passed: lint; strict TypeScript; 10/10 focused V1/V2 definition, reference, canonicalization, ordering, and tampering tests; retained Attempt 4 reload; 210/210 executed full-suite tests across 45 passing files; Next.js production Web build; Worker build; and `git diff --check` with no whitespace errors.
- Attempt 4 remains byte-identical at database SHA-256 `280d616ec1366ab00d81962b73fb5dfefe9e0b97fe9b71a64dbac20afeae8162` and evidence SHA-256 `ea6a994968b93cd1dac2a192e8cb5cf6d409b4d11fc661a87237cd2850401b36`. Attempts 1-3, failed pilot evidence, and both historical Phase B blocker reports also remain unchanged and retain their FAIL / BLOCKED status.
- No model call, Sandbox, replacement Job, remote Fixture mutation, or Phase B evidence file occurred. Exact results are in `REPUTATION_ENTRY_PRELIVE_REGRESSION_PASS.md`.
- Stop for Owner review. Live Phase B execution of either replacement Job requires a separate explicit Owner decision.
- **VERSIONED CONTRACT / TASK-DEFINITION HISTORICAL VALIDATION FIX is BLOCKED at the complete pre-live lint Gate.** The Owner authorized only the historical compatibility fix and a complete regression rerun; Phase B Jobs remained blocked.
- The local immutable definition registry and explicit V2 Contract definition reference passed 10/10 focused evolution/tampering tests. Exact legacy semantic matching also restored the retained Attempt 4 reload, which passed 1/1 without changing its Contract or payload hashes.
- The complete Gate then stopped at lint: `work-contract.ts:333:10` defines `taskDefinition` only for `ReturnType<typeof taskDefinition>`, violating `@typescript-eslint/no-unused-vars`. Strict typecheck, full suite, production builds, and final `git diff --check` were not run after that failure.
- Attempt 4 remains byte-identical at database SHA-256 `280d616ec1366ab00d81962b73fb5dfefe9e0b97fe9b71a64dbac20afeae8162` and evidence SHA-256 `ea6a994968b93cd1dac2a192e8cb5cf6d409b4d11fc661a87237cd2850401b36`. Attempts 1-3, the failed pilot, and the earlier Phase B blocker report also remain unchanged.
- No model call, Sandbox, replacement Job, remote Fixture mutation, or Phase B evidence file occurred. Exact results and the next Owner decision are in `REPUTATION_ENTRY_VERSIONED_CONTRACT_PREFLIGHT_BLOCKED.md`.
- **PHASE B REPLACEMENT TWO-JOB BATCH is BLOCKED at the pre-live regression Gate.** The Owner accepted Phase A Attempt 4 and authorized exactly two replacement Jobs only after complete local validation passed against the Phase B code.
- The initial repository baseline passed lint, strict TypeScript, 205 tests, Web and Worker builds, `git diff --check`, and retained Attempt 4 validation. Local preparation then introduced the newly authorized state-transition and TypeScript build-configuration definitions plus progressive durable lifecycle persistence.
- Mandatory validation of that actual Phase B implementation failed 1/216 tests: the retained Attempt 4 Contract is hash-valid and readable, but `assertReputationEntryWorkContractIntegrity` compares it with the newly replaced current `TEST_AND_FIX` definition and returns `REPUTATION_ENTRY_WORK_CONTRACT_TAMPERING_DETECTED`.
- This is a genuine Contract-definition versioning/backward-compatibility blocker. Attempt 4 remains unchanged at database SHA-256 `280d616ec1366ab00d81962b73fb5dfefe9e0b97fe9b71a64dbac20afeae8162` and evidence SHA-256 `ea6a994968b93cd1dac2a192e8cb5cf6d409b4d11fc661a87237cd2850401b36`.
- Execution stopped before either replacement Job, any model request, Sandbox, remote fixture branch/Commit, or Phase B evidence database. Exact evidence and the next required Owner decision are in `REPUTATION_ENTRY_PHASE_B_PRELIVE_REGRESSION_BLOCKED.md`.
- **PHASE A PERSISTENCE PREFLIGHT ATTEMPT 4 is PASS. PHASE B remains NOT AUTHORIZED.** The Owner authorized only the durable-store constructor cleanup correction, focused Windows lifecycle tests, and one complete new Attempt 4 persistence preflight.
- The existing `ReputationEntryDurableStore` constructor now closes its opened `DatabaseSync` resource if PRAGMA, schema, or historical-marker initialization fails, then rethrows the original initialization error. Its public constructor and idempotent `close()` semantics did not change.
- Focused lifecycle validation passed 4/4 on Windows. The deliberate failed-constructor probe observed WAL and SHM while the initial store was open; after the expected marker-integrity failure both sidecars were absent, the database and containing temporary directory were removed immediately, and no waits or retries were used.
- Attempt 4 independently persisted, deliberately failed, restarted, recovered, recomputed every durable and lifecycle hash, validated exact Contract/Authority/execution/Bundle/Receipt bindings, detected all required tampering and overwrite attempts, recovered the Lease as `REVOKED`, and passed retained-evidence reload.
- Fresh evidence is `test-results/reputation-entry-persistence-preflight-attempt-4.sqlite`, SHA-256 `280d616ec1366ab00d81962b73fb5dfefe9e0b97fe9b71a64dbac20afeae8162`, and `test-results/reputation-entry-persistence-preflight-attempt-4-evidence.json`, SHA-256 `ea6a994968b93cd1dac2a192e8cb5cf6d409b4d11fc661a87237cd2850401b36`. Exact results are in `REPUTATION_ENTRY_PERSISTENCE_PREFLIGHT_ATTEMPT_4_REPORT.md`.
- Attempts 1, 2, and 3 remain permanently FAIL / BLOCKED with their databases, sidecars, reports, hashes, and lineage unchanged. Attempt 4 made zero model calls, created zero Sandboxes, prepared zero replacement branches/Commits, ran zero replacement Jobs, and caused no external effects.
- Stop for Owner review. Phase B, both replacement Jobs, Fixture preparation, model calls, Sandboxes, and later Reputation work require a separate explicit Owner decision.
- **PHASE A PERSISTENCE PREFLIGHT ATTEMPT 3 is FAIL / BLOCKED.** The Owner-authorized canonical command comparison correction passed focused equivalence and mutation tests. The complete new Attempt 3 then persisted, deliberately failed, restarted, recovered, recomputed durable/lifecycle hashes, accepted key-only normalization, and ran the required Contract/command/argument/Source/Authority/Bundle/Receipt tampering probes.
- A new blocker appeared during historical-marker probe cleanup: the expected failing `ReputationEntryDurableStore` constructor left an opened SQLite handle, so Windows returned `EBUSY` while unlinking `marker-proof.sqlite-shm`. The retained PASS JSON was not written and no result is promoted.
- Attempt 3 remains preserved at `test-results/reputation-entry-persistence-preflight-attempt-3.sqlite`, SHA-256 `648340d1e74699cfe052257b696b83977f0ed455e231bcfc231bb82ea27acf15`. It contains 14 durable records, 12 valid lifecycle entries ending at chain SHA-256 `0baa912daba85400c53183964c5b723543bdc4cb8d53cddc10988040d437b17b`, exact Attempt 1/2 lineage, and final Lease state `REVOKED`.
- Attempts 1 and 2, their reports, and the historical marker remain unchanged. Attempt 3 made zero model calls, created zero Sandboxes, ran zero replacement Jobs, and caused no external side effects.
- Phase B remains unauthorized. Exact evidence and the next owner decision are in `REPUTATION_ENTRY_PERSISTENCE_PREFLIGHT_ATTEMPT_3_BLOCKED.md`.
- **CORRECTED PHASE A ATTEMPT 2 is FAIL / BLOCKED.** The Owner authorized only correction of the original cross-domain hash assertion and one rerun using a new evidence database. Phase B remains unauthorized.
- The corrected assertions proved that full persisted Work Contract payload SHA-256 `e3cccb78c243d346a2bf13df71c0d6bf6e8015ec50ba58afdf5d0e34967f8fda` and self-hash-excluded canonical Contract SHA-256 `38b64c2e49dff1525ee52a86df1f77352c047d79b479d5a362250d5e14c4fa27` each recompute over their intended domains and remain distinct after reload.
- A new blocker then appeared: canonical persistence sorts object keys, while `assertReputationEntryWorkContractIntegrity` uses native `JSON.stringify` to compare command objects. Reloaded and expected commands are semantically identical but have different key insertion order, so the validator returned `REPUTATION_ENTRY_WORK_CONTRACT_TAMPERING_DETECTED`.
- Attempt 2 remains preserved at `test-results/reputation-entry-persistence-preflight-attempt-2.sqlite`, SHA-256 `99f924efb22dea0dc1cea2c460f66be2469588771e55f2b66fbc59aa3de6a11c`. It contains 14 durable records, 12 valid lifecycle entries, exact lineage to Attempt 1, and a final `REVOKED` Lease. Its missing JSON summary is not reconstructed.
- Attempt 1 database SHA-256 `ad2a27aa75976d7a3202f6368fd21ccfa15b9c6fe81cb0f967b8f76e97f666af`, original blocker report SHA-256 `33efab2d26727e9a18d108b6d33d91f0c8615000c8b9deceea01cb962cfe3a1c`, and historical marker SHA-256 `5d90116229342b94fa9ef935e2c5780d2a0655143ec121db78b8c58176fd598f` remain unchanged.
- Attempt 2 made zero model calls, created zero Sandboxes, prepared zero replacement branches/commits, and ran zero replacement Jobs. The tampering negative tests did not execute after the new blocker and are not claimed as passed.
- **REPLACEMENT TWO-JOB BATCH is BLOCKED in Phase A.** The Owner accepted the prior pilot as `FAIL / NOT PROMOTABLE` and authorized one replacement batch only after a deterministic persistence preflight passed. That preflight failed, so Phase B did not start.
- Phase A durably wrote one local preflight Job, exact Agent/profile, execution, Work Contract, ACTIVE and REVOKED Lease records, intermediate/final Bundle records, verifier result, Ledger snapshot, Receipt, and a 12-entry lifecycle chain before a deliberate process failure. Restart recovered all 13 Job/global records and the store-level integrity check passed.
- The Gate assertion then incorrectly compared full persisted Work Contract payload SHA-256 `e3cccb78c243d346a2bf13df71c0d6bf6e8015ec50ba58afdf5d0e34967f8fda` with the Contract's self-excluding `workContractSha256` `38b64c2e49dff1525ee52a86df1f77352c047d79b479d5a362250d5e14c4fa27`. These hashes cover different canonical payloads and are not expected to match.
- Because that assertion failed before the retained JSON summary was written, Phase A is `FAIL` even though the SQLite records survived. The SQLite evidence remains at `test-results/reputation-entry-persistence-preflight.sqlite`; it must not be overwritten or presented as a PASS.
- The historical failed-pilot marker remains unchanged at SHA-256 `5d90116229342b94fa9ef935e2c5780d2a0655143ec121db78b8c58176fd598f`.
- This Phase A attempt made zero model calls, created zero Sandboxes, prepared zero replacement Fixture commits, and ran zero replacement Jobs. No retry is authorized under the current decision.
- **TWO-JOB REPUTATION ENTRY CORPUS COLLECTION PILOT V1 concluded as FAIL / NOT PROMOTABLE.** Phase 4 Reputation has not started.
- The approved batch attempted exactly two distinct Jobs: `TEST_AND_FIX` at Fixture Commit `abb6f4e3555acb94a2ea8878b053dec9690e6f89` and `BUILD_RESCUE` at `c31360d1e64c457bdeeea9820840876d9c4b77be`.
- The in-memory pilot outcome was `PARTIAL`: two `REPUTATION_ENTRY_CANDIDATE` records were constructed under one new ACTIVE Agent profile revision, but neither met the pilot's verified-candidate condition. The exact Agent actions and failure reasons were lost when the live test asserted `PASS` before persisting the returned `PARTIAL` outcome.
- Four Vercel Sandboxes are independently visible in Provider metadata: one Agent and one verifier Sandbox per Job. All four are `stopped`, non-persistent, and have no Snapshot. No PR, merge, deployment, payment, or blockchain write occurred.
- The loss is preserved in `test-results/reputation-entry-pilot-attempt-1-recovered.json`. Missing Job IDs, execution IDs, Contract/Lease documents, patches, model usage, Evidence Bundle hashes, Ledgers, and Receipts were not reconstructed or manufactured.
- The persistence path now writes returned outcomes before asserting pilot success, refuses to overwrite any prior attempt marker, and revokes rather than merely completes each future pilot Lease. These corrections were local only; no Job, model request, or Sandbox was rerun.
- Canonical qualifying real Verified Jobs added: **0**. The strict corpus remains **0**, so the exact Reputation entry gap remains **20 real Verified Jobs**, more than one qualifying task type, and enough samples for each claimed capability.
- A replacement evidence batch requires separate Owner approval and must use new, materially distinct source identities; the two consumed Jobs must never be relabeled or rerun as successes.
- **AGENT IDENTITY AND INTEROPERABILITY V1 DESIGN AND EVIDENCE GATE is owner-accepted at one local deterministic boundary.** Phase 4 Reputation has not started.
- Internal identity uses stable opaque Agent ID `agt_01K4M5N6P7Q8R9S0T1V2W3X4Y5`, append-only whole-profile SHA-256 revisions, and an explicit platform-account controller relationship that does not claim legal identity verification.
- The Gate preserved three profile revisions for the same Agent ID: the execution-bound revision, a display-name update, and a disabled revision. The prior profile hash links every new revision, and historical Receipt meaning remains bound to the execution-time snapshot.
- Agent identity and execution identity are distinct. Execution `exe_aFYARMnUCDUum-OWEurk7kpn` binds the exact profile revision/hash, Work Contract, Permission Lease, Job run, Server-side executor, runtime/provider metadata, and execution hash.
- Identity-aware Work Contracts, Permission Leases, repair/delivery execution, Evidence Ledger entries, Gate Receipt, and public Receipt projection now require coherent Agent and execution references. Missing or mismatched identity fails closed before Source materialization or Sandbox creation.
- Ten negative probes deny unknown Agent, wrong-Agent Contract reuse, wrong-Agent Lease reuse, execution mismatch, Receipt rebinding, disabled execution, malformed external ID, conflicting external linkage, treating `DECLARED` as `VERIFIED`, and profile tampering.
- ERC-8004 and HOL/UAID are typed optional references only. The evidence contains declared test vectors, not externally verified identities; no chain, token, registration, payment, live external call, Sandbox, model call, or Repository mutation occurred.
- The 22-entry Evidence Ledger, profile history, execution identity, revoked Lease, and Receipt verify offline. Exact evidence is in `AGENT_IDENTITY_INTEROPERABILITY_V1_REPORT.md`, `test-results/agent-identity-v1-evidence.json`, and `test-results/agent-identity-working-tree-identity.txt`.
- Production identity persistence/API/UX, legal/KYC assurance, live external linkage verification, key ownership challenges, generalized identity migration, public directory, capabilities verification, and Reputation remain unimplemented.
- **TASK-SCOPED AGENT AUTHORITY V1 DESIGN AND EVIDENCE GATE is complete and owner-accepted at one narrow Fixture boundary.**
- `TASK_SCOPED_AUTHORITY_V1` extends the existing Permission Lease with an exact Work Contract ID/version/hash, DoneLayer Server issuer, Agent/executor/Job recipient, Repository/ref/Commit, delivery namespace, allowed file, Sandbox boundary, approval requirements, unsupported areas, and immutable authority hash.
- Work Contract repair now requires the bound Lease before source materialization. Repair Sandbox creation, patch paths, fixed tests, delivery branch, PR creation, and independent-verifier Sandbox are guarded. Missing, expired, revoked, mismatched, mutated, or widened authority fails closed.
- The live Gate issued a five-minute Lease for exact Commit `5ae6dd136b99c4727e8ac50833a716cfbbf4ae00`, authorized one read-only GitHub identity check, recorded seven denied probes, revoked the Lease, and issued a 14-entry integrity-valid authority Receipt.
- No Sandbox, model call, repository mutation, branch, Commit, PR, merge, deployment, or payment was created by this Gate.
- Exact evidence is in `TASK_SCOPED_AGENT_AUTHORITY_V1_REPORT.md`, `test-results/task-scoped-authority-v1-evidence.json`, and `test-results/task-scoped-authority-working-tree-identity.txt`.
- Production approval UX/signatures, task-specific credentials, general persistence, arbitrary repositories, model-request budget enforcement, organization/data/external-service policy, real payment, and later phases remain unimplemented.
- **VERIFIED WORK CONTRACT V1 DESIGN AND EVIDENCE GATE is complete at one narrow Fixture boundary.** Its owner review was accepted before the current Task-Scoped Agent Authority Gate began.
- `VERIFIED_WORK_CONTRACT_V1` locks owner-authoritative acceptance criteria, Source identity, allowed/forbidden actions, required Evidence, deterministic verifier policy, explicit outcomes, and a whole-contract hash.
- `VERIFIED_WORK_EVIDENCE_BUNDLE_V1` records required, satisfied, and missing Evidence. `VERIFIED_DELIVERY` requires a complete projection plus valid execution, test, Contract, and delivery dimensions.
- Agent-reported and independently verified completion are separate Receipt states. Review-only evidence cannot claim verified delivery.
- The live Gate repaired exact Fixture Commit `5ae6dd136b99c4727e8ac50833a716cfbbf4ae00`, changed only `src/add.ts`, and produced patch SHA-256 `b9aee74ff52dfe2f5a7d1c7bd478b333d656b6f4ce68b77212f5d863f441c1db`.
- The model completed eight bounded repair tool calls, but its follow-up request failed with `AI_GATEWAY_MODEL_REQUEST_FAILED`. The Agent run remains `FAILED`; DoneLayer's separate repair evaluation is `VERIFIED`. Token counts and exact Gateway cost were unavailable and remain null.
- The exact repair was delivered to branch `donelayer/repair/c30fdf0e687d`, Commit `3c96382fc6e8a2f154ad422286b6cf63c75397b3`, and real GitHub PR #2. PR #2 is `OPEN` and unmerged, targets only `fixture/real-source-bug-v1`, and must not be merged by this Gate.
- A fresh `NON_AI_DETERMINISTIC_VERIFIER` Sandbox passed install, 4/4 repository tests, four locked Contract assertions, Source/test/configuration integrity, and cleanup. Build was truthfully `NOT_PRESENT`.
- Evidence Bundle `bedc4785e44968606fc512996799102882a74ee4f98845fadecb4532ee7e5dd9` satisfies all nine required Evidence categories and is bound into the valid delivery/public Receipt.
- The Test Ledger released only simulated funds: `$8.00` Provider and `$2.00` platform from a simulated `$10.00` charge. No real payment occurred.
- The initial source-boundary failure remains preserved; its Sandbox was stopped and it made zero Agent calls and authorized no payout. Exact history is in `VERIFIED_WORK_CONTRACT_V1_BLOCKED.md`.
- Exact successful evidence is in `VERIFIED_WORK_CONTRACT_V1_REPORT.md`, `test-results/semantic-verification-real-evidence.json`, `test-results/semantic-verification-reality.sqlite`, `test-results/semantic-true-repair.patch`, and `test-results/semantic-delivery-diff.patch`.
- Portable Attestation Envelope V1 has not started.
- Manual external review found that the historical Fixture test requires `add(2, 2) = 5` while ordinary arithmetic requires `4`. The historical GitHub delivery remains valid evidence of execution, test conformity, delivery, and cleanup, but is not proof of Task Contract conformity.
- Historical PR #1 remains `OPEN` and unmerged. It must not be modified, closed, or merged by this Gate.
- The isolated true-source-bug Fixture branch is `fixture/real-source-bug-v1` at exact Commit `5ae6dd136b99c4727e8ac50833a716cfbbf4ae00`. It changes the Fixture implementation to subtraction and locks correct repository tests for the four arithmetic examples. This branch is an intentionally broken test fixture, not a product repair.

- The Worker Reality Gate is complete for the fixed, local `WORKER_SMOKE_V1` infrastructure workflow.
- Trust Foundation v1 is complete for that same fixed local Worker Infrastructure Verification boundary.
- Real Repository Materialization Gate v1 is complete for the exact public allowlist `https://github.com/NickHOI/donelayer-build-rescue-fixture.git` on `main`.
- Managed Remote Sandbox Gate v1 is complete for the fixed Vercel smoke and timeout probe.
- **REAL BUILD AND TEST IN MANAGED SANDBOX GATE V1 is complete** for one freshly resolved exact Fixture commit: `600f326ce373160eb5495aaef72230d4c9807e8f`.
- The Server created a six-file Source Manifest and bounded canonical Source Package, and the Sandbox independently reverified the Repository, Commit, Manifest and package hashes before extraction.
- Real `npm ci --ignore-scripts --no-audit --no-fund` exited `0`. The exact Fixture has no build script, so build is truthfully `NOT_PRESENT`; no build success was fabricated.
- Real `npm test` exited `1` on the intentional Fixture assertion. Task status is `VERIFICATION_FAILED`, Job and Receipt results are `FAILED`, while Receipt integrity is independently `VALID`.
- All 18 Artifacts have matching claimed/Server hashes and strict content cross-bindings. The 78-entry Ledger is valid; no Test Ledger release occurred.
- The Vercel Sandbox is `stopped`, non-persistent, without a Snapshot, and cleanup is verified. No local workload/Docker fallback or Codex repair occurred.
- Exact evidence is in `REAL_BUILD_TEST_SANDBOX_REPORT.md`, `test-results/real-build-test-sandbox-evidence.json`, and `test-results/real-build-test-sandbox.sqlite`.
- **AGENT REPAIR IN MANAGED SANDBOX GATE V1 is complete** for the same exact Fixture Commit. The separately authorized recovery used `VercelAIGatewayAgentProvider` and the free live model `inclusionai/ling-3.0-flash-fin`.
- The real model made eight successful DoneLayer tool calls, changed only `src/add.ts`, left tests unchanged, and produced patch SHA-256 `6e8679bbef6dbbba31f84ffb20b209df4d3fadfee81e58383e9926a70855c996`. The real repaired test exited `0`.
- Four model requests completed the repair. The final text-only response request and its one allowed retry returned `429` after all required repair evidence existed; this is recorded without changing the verified Repair Outcome.
- The recovery used `$0.000375` of existing Gateway credit. The Sandbox is stopped, non-persistent, and has no snapshot. The 13 Artifacts, 34-entry full Ledger, Receipt, public Receipt, and SQLite records verify consistently.
- Exact evidence is in `AGENT_REPAIR_SANDBOX_REPORT.md`, `test-results/agent-repair-sandbox-evidence.json`, `test-results/agent-repair-sandbox.sqlite`, `test-results/agent-repair.patch`, and `test-results/agent-repair-gateway-postflight.json`. The earlier poolside failure remains preserved in `AI_GATEWAY_AGENT_REPAIR_BLOCKED.md` and `test-results/agent-repair-sandbox-blocked-v1-evidence.json`.
- **GITHUB PATCH DELIVERY AND INDEPENDENT VERIFICATION GATE V1 is complete** for that exact persisted repair patch and owner-controlled Fixture.
- The exact patch was delivered to deterministic branch `donelayer/repair/ed564b18-8fa2`, Commit `466be67975a953a1ae3b5cfc41e80d42ee057164`, and real GitHub PR #1. The parent is the exact Base Commit. The PR is `OPEN` and unmerged.
- `NON_AI_DETERMINISTIC_VERIFIER` independently re-materialized the exact Remote Delivery Commit into a new Vercel Sandbox, ran `npm ci` with exit `0`, truthfully recorded build `NOT_PRESENT`, and ran `npm test` with exit `0` and 2/2 tests passed.
- Tests, the package test script, and test configuration match the Base Commit. No GitHub, Vercel, or AI credential entered the Sandbox. It is stopped, non-persistent, and has no Snapshot.
- This delivery Gate made 0 AI/model calls and used `$0` AI Gateway credit. The valid `VERIFIED_DELIVERY` Receipt authorized the Test Ledger to release only simulated funds: `$8.00` Provider and `$2.00` platform from a `$10.00` simulated customer charge.
- The first live attempt stopped before Sandbox creation on an offline final-newline comparison error; its valid failed Receipt and unreleased Test Ledger are preserved. The corrected resume reused the exact existing branch/Commit/PR idempotently.
- Exact evidence is in `GITHUB_DELIVERY_INDEPENDENT_VERIFY_REPORT.md`, `test-results/github-delivery-independent-verify-evidence.json`, `test-results/github-delivery-independent-verify.sqlite`, `test-results/github-delivery.json`, and `test-results/delivery-diff.patch`.
- Work now stops for owner review. No PR merge, deployment, real payment, generalized GitHub delivery, or later product phase is authorized by this result.
- `PRODUCT_ROADMAP.md` now records Verified Work as the immediate wedge and preserves task-scoped Authority, Agent Identity/interoperability, evidence-based Reputation, controlled Payments, settlement, organizational trust, and the Agent Work Market as the long-term platform. This is a documentation decision only; none of those future phases was started or made REAL by the roadmap update.

## Owner Decision

- `LOCAL_SELF_HOSTED_DOCKER`: `DEFERRED_BY_OWNER`; required for default MVP: **No**.
- Local Docker / VM remains opt-in Advanced Provider / Enterprise Self-hosted Mode, not a requirement for ordinary users.
- The historical Docker installation request is superseded by the architecture decision, not by a successful Docker test.
- Read `adr/ADR-MANAGED-REMOTE-SANDBOX.md` for scope, trade-offs, provider boundaries, and cost limits.

## Current Boundary

- Agent Identity V1 now includes the versioned domain model, identity-aware Contract/Lease/execution/Receipt binding, and local durable Marketplace SQLite profiles with controlled API/read UX. It is not Supabase/multi-instance production persistence or a deployed production identity service.
- Platform controller assurance means only that DoneLayer records a platform account/control relationship. It does not mean the person, organization, or company has been legally verified.
- `DECLARED` and `OBSERVED` external identity references are never promoted to `VERIFIED` without a defined method, evidence hash, and verification timestamp. This Gate performed no such external verification.
- Identity existence does not grant authority, and identity metadata does not create Reputation. The Permission Lease remains the authorization source for a Work Contract.
- Task-scoped authority is REAL only for the exact V1 schema/guard, local negative tests, captured read-only Fixture action, five-minute Lease, revocation, Ledger, and Receipt. It is not a general production permission service.
- PR creation requires distinct prior Owner approval evidence. PR merge and production deployment remain explicitly denied and were not attempted.
- The live authority Gate used owner development GitHub authentication only in the Server process. It minted no credential and exposed none to a workload.
- The completed Gate inspected exact Commit `600f326ce373160eb5495aaef72230d4c9807e8f` and proved `SPEC_TEST_CONFLICT` with zero Agent calls and zero repair Sandbox attempts.
- The completed Gate executed one bounded Agent repair against exact Fixture Commit `5ae6dd136b99c4727e8ac50833a716cfbbf4ae00`, delivered the source-only repair to a dedicated `donelayer/repair/*` branch, and opened an unmerged Pull Request targeting `fixture/real-source-bug-v1`.
- Semantic success requires repository tests and independent DoneLayer-controlled assertions derived from the locked Task Contract to pass in the repair Sandbox and a second fresh non-AI Sandbox. Tests and acceptance criteria must remain unchanged.
- Simulated payout release requires valid execution, repository test conformity, Contract conformity, delivery integrity, fresh verification, cleanup, and a valid Evidence chain. No real payment is authorized.
- Historical evidence is immutable. A semantic correction must be append-only and may mark the historical semantic-success claim `SUPERSEDED` while keeping historical Receipt integrity and execution evidence `VALID`.

- GitHub clone, managed install/test, bounded Agent repair, exact-patch delivery, and clean independent verification are REAL only for the exact allowlisted Fixture and the specific Commits, patch, branch, PR, and Gate runs recorded above.
- This Commit has no build script. The Gate proves conditional build detection, not a successful build execution.
- Test statistics remain formally `UNPARSABLE`; the raw log and exit `1` are preserved without guessed counts.
- General Customer-selected repository access and production GitHub App installation grants are not approved by these Gates. Delivery authentication was explicitly `OWNER_DEVELOPMENT_GITHUB_AUTH`, not a production GitHub App.
- Agent Repair is REAL only for the exact allowlisted Fixture Commit, the recorded free Gateway model, the bounded DoneLayer tool set, and this single authorized recovery run.
- No local workload, local Docker, Demo, or second-Provider fallback is approved.
- Platform credentials stay exclusively in the Server-side Orchestrator; use development OIDC, never inject secrets or local project/home files into the sandbox.
- Dependency network is initially deny-all, opens only to exact `registry.npmjs.org` for `npm ci`, and returns to deny-all before test/source verification.
- Require a non-persistent lifecycle without snapshots, bounded resources/output, strict Source integrity, hash/content-bound Evidence, and verified cleanup before any Receipt.
- Stop at the first authentication, linking, OIDC, availability, or billing blocker. Report exactly one manual next step. No plan upgrade, billing change, credit purchase, or paid add-on.
- No Attempt 5 Job retry, paid plan upgrade, credit purchase, billing change, or second Agent provider is approved.
- The one recorded Fixture branch, Commit, and open PR are approved and complete; merge, deployment, deletion, and any other Repository delivery remain unapproved.
- Real payment integration is not approved.
- `WORKER_SMOKE_V1` must not be described as a real Customer coding Job.
- Capability Passport, Contextual Reputation, external Agent protocol execution, C2PA and the AI Transparency Kit remain gated or deferred.
- The authorized next work is Gate 4A corpus entry, not Reputation implementation. Phase 4 `EVIDENCE-BASED REPUTATION` starts only after Gate 4C reaches at least 20 active canonical real Verified Jobs and passes its independent Readiness Review.
- Gate 4A-P changes infrastructure source only. Before any Gate 4A Contract lock, the Owner must review the new immutable baseline, authorize non-production migration apply/readback after CLI authentication, and approve the candidate's exact new Commit/tree source binding. The current DRAFT fingerprint is preserved as historical approval evidence and is not silently rewritten.

Stop after `REPUTATION_ENTRY_PHASE_B_ATTEMPT_5_REPORT.md`. Do not mutate or
rerun Attempts 3-5, push replacement Fixture commits, execute another Job, call
a model, create a Sandbox, promote a candidate, begin Reputation, or start any
later Gate without a new Owner decision. Attempt 5 classification is `DO NOT
COUNT`; the immediate next action requires separate authorization for a
genuinely new real-work corpus Gate.

The preceding stop text is retained as the historical Attempt 5 boundary and
was superseded operationally by Controlled Autopilot Protocol V2 on 2026-09-08.
That authorization permitted genuine roadmap product work without changing the
Attempt 5 classification or Phase 4 entry rules. The Phase 3 SQLite durable
productization and Supabase/Postgres local contract milestones were subsequently
externally accepted. Live migration validation still requires a separately
authorized non-production Supabase project boundary.

The later Owner decision on 2026-09-13 supersedes only the ambiguous Gate 4
execution-boundary stop. It authorizes `GATE_4_REAL_CORPUS_RAMP_V1` and Gate 4A;
it does not relabel any historical work, create a corpus entry, start
Reputation, or weaken the Real Job classification and external-audit policy.
